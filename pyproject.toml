[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "batchsweep"
version = "0.2.0"
requires-python = ">=3.9"
dependencies = ["torch"]

[project.optional-dependencies]
dev = ["xgboost", "scikit-learn"]
